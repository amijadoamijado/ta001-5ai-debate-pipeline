# ORDER.md - タスク指示テンプレート

このテンプレートをコピーして、タスクごとに埋めてください。

---

## タスク概要

**何をするか**（1〜2行で）
<!-- 例: ユーザー認証機能を実装する。ログイン画面とJWTトークン発行APIを作成。 -->

**優先順位**
- [ ] High / Medium / Low

**担当モデル**
- [ ] Claude Code / Codex / Gemini CLI / Antigravity

---

## 作業範囲

**対象ファイル/ディレクトリ**
```
src/auth/
tests/auth/
```

**新規作成するもの**
- [ ] `src/auth/login.ts`
- [ ] `src/auth/jwt.ts`
- [ ] `tests/auth/login.test.ts`

**既存の変更禁止リスト**
- [ ] `src/core/` は変更禁止
- [ ] 既存のインターフェースは変更禁止

---

## 完了条件

**必須条件**（全て満たすこと）
- [ ] `npm test` が全パスする
- [ ] `npm run lint` がエラーなし
- [ ] `npm run build` が成功する

**受け入れテスト**
```bash
# このコマンドが成功すれば完了
npm test -- --testNamePattern="login"
```

**動作確認**
- [ ] ログイン画面が開く
- [ ] 正しいパスワードでトークンが発行される
- [ ] 間違ったパスワードで401が返る

---

## 参照情報

**関連仕様書**
- `.kiro/specs/xxx/requirements.md` - セクション3.2
- `.kiro/specs/xxx/design.md` - 認証アーキテクチャ

**関連ファイル**
- `src/types/auth.ts` - 型定義
- `src/config/auth.ts` - 設定

**類似実装**
- `src/auth/old-login.ts` - 旧実装（参考用）

---

## やってはいけないこと

- [ ] データベーススキーマを変更しない
- [ ] フロントエンドフレームワークを変更しない
- [ ] 既存の認証ロジックを削除しない
- [ ] ハードコードされた秘密鍵を使わない

---

## 追加コンテキスト

<!-- 自由記述エリア -->
- 現在の認証はセッションベース。JWTへの移行は段階的に。
- トークンの有効期限は1時間。
- リフレッシュトークンは次のスプリントで。

---
