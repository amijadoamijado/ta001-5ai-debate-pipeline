# {{PROJECT_NAME}} - Gemini CLI設定

## 利用可能コマンド

### 仕様書駆動開発（序盤）
```
/sd:spec-init {feature}           # 仕様書初期化
/sd:spec-requirements {feature}   # 要件定義
/sd:spec-design {feature}         # 技術設計
/sd:spec-tasks {feature}          # タスク分解
```

### 実装（中盤）
```
/sd:spec-impl {feature}           # TDD方式で実装
/sd:spec-status {feature}         # 進捗確認
```

### 検証（終盤）
```
/sd:validate-design {feature}     # 設計検証
/sd:validate-gap {feature}        # ギャップ分析
/sd:validate-impl {feature}       # 実装検証
```

### ステアリング
```
/sd:steering                      # ステアリング文書作成
/sd:steering-custom               # カスタムステアリング
```

## 開発フェーズ戦略

| フェーズ | 戦略 | 主要コマンド |
|---------|------|-------------|
| 序盤 | 仕様書ファースト + フロントエンド駆動 | spec-init, spec-requirements, spec-design |
| 中盤 | AI協調ワークフロー + 高速イテレーション | spec-impl, spec-status |
| 終盤 | 対話型解決法 + 本番モード | validate-* |

## Core Principles
- Always start complex tasks in plan mode. Iterate on the plan until
  it's solid before executing.
- After any correction, confirm with user before updating configuration
  to prevent repetition.
- Challenge changes: Justify every edit, prove it works, and compare
  against main/master branch.
- Use subagents for parallel compute and to keep main context clean.
  Offload subtasks like verification or cleanup.

## Workflow Orchestration
- Run parallel sessions using git worktrees for isolation.
- For bugs: Provide full context (e.g., logs) and
  delegate end-to-end.
- Use reusable skills/commands for daily repeats.

## Environment
- Environment: Windows 11, UTF-8 (BOM), CRLF.
- Context management: 10% remaining -> save session, 5% -> emergency save.

---

## Common Rules

**MUST READ**: `.handoff/RULES.md`

## File Location Rules

| カテゴリ | ファイル | 保存先 |
|---------|---------|--------|
| 仕様書 | requirements.md | `.sd/specs/{feature}/` |
| 仕様書 | design.md | `.sd/specs/{feature}/` |
| 仕様書 | tasks.md | `.sd/specs/{feature}/` |
| ワークフロー | IMPLEMENT_REQUEST | `.sd/ai-coordination/workflow/spec/{projectID}/` |
| ワークフロー | REVIEW | `.sd/ai-coordination/workflow/review/{projectID}/` |

**配置禁止**: プロジェクトルート直下への新規作成

---
SD003 v2.13.0 | 導入日: {{DATE}}
